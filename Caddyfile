:{$PORT} {
    # Serve static files from the public directory if they exist
    root * /public
    file_server

    # Reverse proxy any requests that didn't match a static file to the Node.js backend
    handle {
        reverse_proxy localhost:{$NODE_PORT}
    }
}
