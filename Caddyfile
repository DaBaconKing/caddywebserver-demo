:80 {
    # Set the root directory for static files
    root * /public

    # Try serving the static file first. If not found, pass through to the next handler
    file_server browse

    # Proxy requests that were not handled by file_server to the Node.js backend
    handle {
        reverse_proxy localhost:8080
    }
}
